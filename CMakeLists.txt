# Autogenerated by auto2cmake on 2018-03-07 11:49:59
cmake_minimum_required(VERSION 3.11)

project( matio
         VERSION 1.5.11
         LANGUAGES C CXX #Fortran
)

find_package(Git)
if(Git_FOUND)
  message(STATUS "Git found: ${GIT_EXECUTABLE}")
  else()
  message(FATAL_ERROR "Git NOT found")
endif()


include(FetchContent)
FetchContent_Declare(
  matio-download
  GIT_REPOSITORY    https://github.com/tbeu/matio.git
  GIT_TAG           v1.5.11
)

FetchContent_GetProperties(matio-download)
if(NOT matio-download_POPULATED)
  FetchContent_Populate(matio-download)
  # add_subdirectory(${matio-download_SOURCE_DIR} ${matio-download_BINARY_DIR})
endif()


# Installation options
include(GNUInstallDirs)

include(cmake/options.cmake)
include(cmake/thirdParties.cmake)
include(cmake/compilerOptions.cmake)

include(cmake/xxxxx.cmake)

include(cmake/src.cmake)
# include(cmake/getopt.cmake)
# include(cmake/tools.cmake)
# include(cmake/test.cmake)
add_dependencies(matio matio-download)

include(cmake/packaging.cmake)
